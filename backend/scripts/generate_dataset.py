"""
DEPRECATED — superseded by export_for_colab.py.

This script was used during early development. It has been replaced by
`export_for_colab.py`, which uses the backend's own preprocessing pipeline
and produces properly formatted outputs for Colab training.

To regenerate the training data:
    python scripts/export_for_colab.py

This file is retained only to avoid breaking cached __pycache__ references.
"""

raise SystemExit(
    "This script is deprecated. Use: python scripts/export_for_colab.py"
)
