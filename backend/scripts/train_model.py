"""
MOVED TO COLAB — training is done in Google Colab, not here.

See: sqli_colab_training.ipynb  (at project root)

Workflow:
1. Run:  python scripts/export_for_colab.py
2. Upload colab_export/vocabulary.json + colab_export/training_data.npz to Colab
3. Open sqli_colab_training.ipynb and run all cells
4. Download sqli_model.npz → place in backend/app/model/weights/sqli_model.npz
5. Restart the backend — inference is now live

This file is kept as a placeholder so the scripts/ directory is complete.
"""

print(__doc__)
